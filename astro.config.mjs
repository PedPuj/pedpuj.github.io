// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // ─────────────────────────────────────────────────────────────
  //  YOUR SITE ADDRESS
  //  This is the ONLY line that changes if you add a custom
  //  domain later. Swap it for e.g. 'https://www.bedrofoto.com'
  //  and follow the steps in NOTES.md.
  //
  //  No "base" setting is needed: the repo is named
  //  PedPuj.github.io, so the site lives at the root of this
  //  address rather than in a subfolder.
  // ─────────────────────────────────────────────────────────────
  site: 'https://pedpuj.github.io',
});
